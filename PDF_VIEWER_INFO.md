# 📄 Встроенный просмотрщик PDF

## 🎯 Что было сделано:

### 1. **Создана страница просмотра `/viewer`**
- Новая страница для встроенного просмотра PDF файлов
- Файлы открываются внутри сайта, а не в новой вкладке
- Нет возможности скачать файлы (защита контента)

### 2. **Обновлен компонент FileCard**
- ❌ Удалена кнопка "Скачать"
- ✅ Одна кнопка "Открыть для просмотра"
- ✅ Открывает файлы через `/viewer`
- ✅ Передает URL и название файла

### 3. **Обновлена главная страница**
- Кнопка "Программа курса" → открывается через viewer
- Кнопка "Перейти к тестам" → открывается через viewer

---

## 🔧 Как это работает:

### Страница viewer (`/app/viewer/page.tsx`)

```
/viewer?url=<URL_файла>&title=<Название>
```

**Параметры:**
- `url` - закодированный URL PDF файла
- `title` - название документа (отображается в шапке)

**Особенности:**
1. **Встроенный iframe** с параметрами безопасности:
   - `#toolbar=0` - скрывает панель инструментов
   - `#navpanes=0` - скрывает навигационную панель
   - `#scrollbar=0` - скрывает скроллбар

2. **Кнопка "Назад"** - возврат на предыдущую страницу

3. **Полноэкранный режим** (desktop):
   - Кнопка "Полный экран" / "Выход"
   - Разворачивает viewer на весь экран

4. **Адаптивность**:
   - Responsive дизайн
   - Работает на всех устройствах
   - На мобильных скрывается кнопка полного экрана

---

## 🔒 Защита от скачивания:

### Что сделано:
1. ✅ Убраны все кнопки "Скачать"
2. ✅ Скрыта панель инструментов PDF (toolbar=0)
3. ✅ Файлы открываются в iframe

### Ограничения:
⚠️ **Важно:** Полностью запретить скачивание PDF в браузере невозможно, т.к.:
- Пользователь может открыть DevTools
- Можно скопировать URL из адресной строки
- Браузер всегда загружает файл для отображения

### Если нужна полная защита:
Для настоящей защиты от скачивания нужно:
1. Конвертировать PDF → изображения на сервере
2. Использовать watermark (водяные знаки)
3. Использовать DRM (Digital Rights Management)
4. Прокси-сервер для скрытия реальных URL

---

## 📦 Файлы изменений:

### Новые файлы:
- ✅ `/app/viewer/page.tsx` - страница просмотра PDF

### Измененные файлы:
- ✅ `/components/file-card.tsx` - обновлены кнопки
- ✅ `/app/page.tsx` - ссылки через viewer

---

## 🎨 Интерфейс viewer:

```
┌─────────────────────────────────────────┐
│ ← Назад | Название документа | [⛶]    │ ← Header
├─────────────────────────────────────────┤
│                                         │
│                                         │
│           PDF CONTENT                   │
│         (встроенный iframe)             │
│                                         │
│                                         │
└─────────────────────────────────────────┘
```

**Кнопки:**
- **← Назад** - возврат на предыдущую страницу
- **⛶ Полный экран** - режим полного экрана (только desktop)

---

## 🚀 Как использовать:

### Из компонента FileCard:
```tsx
// Автоматически работает для всех PDF файлов
<FileCard
  number={1}
  title="Лекция 1"
  description="Описание"
  fileType="pdf"
  fileUrl="https://..."
/>
```

### Прямая ссылка:
```tsx
<Link href={`/viewer?url=${encodeURIComponent(fileUrl)}&title=${encodeURIComponent("Название")}`}>
  Открыть файл
</Link>
```

---

## ✅ Преимущества:

1. ✨ **Единый стиль** - все файлы открываются одинаково
2. 🔒 **Контроль** - нет кнопок скачивания
3. 📱 **Мобильность** - работает на всех устройствах
4. ⚡ **Быстро** - не нужно загружать отдельное приложение
5. 🎨 **Красиво** - интегрировано в дизайн сайта

---

## 🔮 Возможные улучшения:

1. **Добавить прогресс-бар** загрузки PDF
2. **История просмотренных** документов
3. **Закладки** на страницы
4. **Поиск по тексту** внутри PDF
5. **Комментарии** к документам
6. **Конвертация в изображения** для полной защиты

---

**Готово! Теперь все PDF файлы открываются встроенно на сайте! 🎉**

